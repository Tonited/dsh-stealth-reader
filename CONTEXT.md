# CONTEXT — dsh-stealth-reader

本文件是本项目的**词汇表**：定义领域内的术语，仅此而已。不含实现细节、不含方案、不含计划。

## 领域术语

### 阅读对象

**书籍（Book）**
用户导入并可以阅读的一部作品，是书架的最小单位。一本书由若干章节组成。

**章节（Chapter）**
书籍内部的一个阅读单位。用户可以跳到某一章开始读。章节由导入过程划分，之后不再改变。

**书架（Shelf）**
用户已导入的全部书籍的集合，以及每本书当前的阅读进度。书架是导入结果唯一出现的地方。

**纯图片书籍（Image-only Book）**
内部内容几乎全部由图片构成、没有可读文字的书籍（典型是漫画）。本项目**不支持**这类书籍。

**加密书籍（Protected Book）**
需要密钥才能读取正文的书籍（典型是从电商购买的 epub）。本项目**不支持**这类书籍，导入时会明确失败。

### 阅读行为

**阅读（Reading）**
用户逐章阅读一本书正文的行为。阅读期间，用户看到的是自己导入的书籍，不是 DSH 会话。阅读**始终**以伪装形态发生（见「伪装阅读」）。

**阅读进度（Progress）**
用户在某一本书中读到的位置。进度的定义必须与字号、窗口尺寸无关：它锚定在"哪一章、章内大约哪个位置"，而不是屏幕上的像素位置。

**续读（Resume）**
重新打开一本书时回到上次的阅读进度。除非进度已在书的结尾附近，否则续读总是自动发生。

**章节跳转（Chapter Navigation）**
用户在阅读中直接跳到某指定章节的能力。属于阅读的组成部分。
有两种形态：`←` `→` 逐章前后（一次一章），以及在书架的**步骤清单**里点选任意一章（ADR-0008）。

**步骤清单（Step List）**
书架上某本书展开后的章节目录。伪装成「recent tasks」里一个任务展开出来的步骤：
读过的打 `✓`、当前这条用 `▸`、右边是步骤序号。
它是本项目里**单次暴露面积最大**的界面 —— 会把全书的章节标题同时摆出来。

**书内搜索（Full-text Search）**
用户在整本书的正文里查找一段文字的能力。本项目**不做**。

### 隐蔽机制

**阅读器（Reader）**
阅读期间盖住会话区的那层东西的总称。它只有两种状态（见 ADR-0006）。

**伪装内容流（Stream）**
`stream` 态：正文以**任务流**的形态呈现，与工作痕迹行交错、共用同一套排版，逐字长出来。
这是本项目**唯一**的阅读形态，不存在"书页模式"，也**没有**中间的"紧急隐藏态"。

**工作痕迹行（Work Trace Line）**
内容流里那些看起来像真实工作输出的行（工具调用、命令摘要、经脱敏的会话片段）。
它们的职责是让屏幕看起来像 DSH 正在正常跑任务。**优先取自真实会话数据**，通用模板只用于补齐。

**任务流（Task Stream）**
`stream` 态的实际形态：一串自上而下的行，其中**正文段落是主体**，工作痕迹行按固定节奏穿插其间。
节奏必须对同一章稳定，否则读者的位置感会漂移。

**正文片段（Excerpt）**
工作痕迹行里可能出现的、来自用户真实会话的短文本。它的存在是为了让伪装更可信，代价是它会把用户真实在做的事情显示在屏幕上。

**切换（Toggle）**
`closed` 与 `stream` 之间的转换。

**快速切换（Quick Hide）**
用户按快捷键，界面立刻减少暴露（正在阅读则正文立刻消失），且不丢失阅读进度。
这是本项目最重要的单一保证。另一个入口是**鼠标**：动 1px 或点一下立刻回到 DSH 真实界面 ——
手碰鼠标总是比找键盘快，慌乱时这一点是决定性的。

**书单鼠标容忍（List Mouse Tolerance）**
书单在屏幕上时鼠标**不**触发快速切换。理由：书单只能用鼠标操作（点书名打开、点 `✕` 删除、
拖文件导入），一动就收场等于根本点不到书。此时快捷键是唯一的出口。见 ADR-0007。
"在屏幕上"包括**没有打开的书**的情形 —— 那时书单就是覆盖层的全部内容。
它是 `stream` 内部的一个子标记，**不是**第三个界面状态，也不改变默认的鼠标契约。

**兜底入口（Fallback Entry）**
不依赖键盘即可发起切换的入口。它的存在是因为没有任何全局键位能对第三方输入法免疫。
兜底入口是命令面板里的 `/stealth`，**直达 `stream`**（它需要打字，不存在误触风险）。

### 导入

**导入（Import）**
把一本书从用户的文件系统取进书架的过程，包含读取、识别编码、划分章节、落库。

**编码探测（Encoding Detection）**
判断一个文本文件到底是以哪种字符编码写成的过程。探测失败会让正文变成乱码，是中文文本文件最常见的失败方式。

**章节切分（Chapter Splitting）**
把一本书的正文划分为章节的过程。当正文里找不到章节标记时，退化为按长度均分。

**取消导入（Aborted Import）**
没有走完的导入。取消导入的书**不进入书架**——书架里不出现半本书。

## 与其他概念的区别（避免混用）

- **`closed` 与 `stream`** 是两个互斥的界面状态，没有第三种（ADR-0006）。
- **伪装内容流**不是"把小说排版得好看"；它是**唯一**的阅读形态，正文与工作痕迹共用一套排版（对比：书页排版是刻意不做的）。
- **鼠标一动就退出**，而不是"降级到某个更安全的伪装态" —— 被撞见时没有时间判断当前状态。
  唯一例外是书单开着时（ADR-0007）：那是要用鼠标操作的界面，收场交给快捷键。
- **工作痕迹行**不是装饰：优先取真实会话数据，通用模板只是补齐尾部的手段。
- **阅读进度**不是"滚动位置"；后者是实现手段，前者是对用户可见的语义。
- **章节**由导入时确定，不是用户可编辑的对象。
- **书架**只包含导入成功的书；取消导入的书不在其中。
